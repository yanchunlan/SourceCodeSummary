一.	流程
	https 涉及对称加密，非对称加密2种加密算法
	首先非对称加密，把公私钥给CA认证，返回认证后的公私钥，（公钥客户端用，私钥服务端用）
	客户端请求，服务器下发经过CA认证的公钥，客户端CA认证，是否ok（域名，时间，等等）
	认证ok，就公钥加密，对称算法和信息，给服务端，服务端私钥解密
	后面传输就使用对称加密算法传输内容，（主要是对称加密算法传输快，非对称慢，所以非对称第一次验证一次即可）
二.	加密算法
	对称加密 AES,DES
	非对称加密 RSA